'use strict';

//let Add = require('./Subtree/Add');

class FactoryPackage {

    createMenu(type) {
        switch (type) {
            case "add":

        }
    }

}

module.exports = FactoryPackage;